<% if(rs.length > 0) { %>
Title: <%= rs[0].movie_title %>
<ul>
    <% for(var i=0; i < rs.length; i++) {%>
    <li><%= rs[i].genre_name %></li>
    <% } %>
</ul>
<% } else { %>
No movies rated by user id <%= user_id %>.
<% } %>